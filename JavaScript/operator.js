const array = [];
let text = array === 0 ? "배열이 비어있음" : "배열이 비어있지 않음";
console.log(text);

const condition1 = false;
const condition2 = false;

const value = condition1 ? "와우!" : condition2 ? "blabla" : "foo";

console.log(value);
